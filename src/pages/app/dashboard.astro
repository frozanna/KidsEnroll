---
// Parent Dashboard page shell. Embeds interactive React component.
// Access control (auth/role) assumed handled in middleware.
import Layout from "../../layouts/Layout.astro";
import ParentDashboardPage from "../../components/dashboard/ParentDashboardPage";
import ParentNavbar from "../../components/dashboard/ParentNavbar";
export const prerender = false; // SSR only, dynamic user-specific data.
---

<Layout>
  <ParentNavbar client:load activeTabLabel="Panel rodzica" />
  <main class="mx-auto max-w-5xl px-4 py-8" aria-labelledby="parent-dashboard-heading">
    <ParentDashboardPage client:load />
  </main>
</Layout>
