---
export const prerender = false;
// Simple server-side redirect to activities list per implementation plan
return Astro.redirect("/admin/activities");
---

<!-- Redirecting to /admin/activities -->
